import React, { useEffect, useRef } from "react";
import "./inGameCanvas.css";

const UserState = ({ name, state }: any) => {
  return <svg className="border border-gray-500 canvas-size "></svg>;
};

export default UserState;
