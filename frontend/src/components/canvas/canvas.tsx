import React, { useEffect, useRef } from "react";
import "./canvas.css";

// function Canvas() {
//   return <canvas className="canvas-size"></canvas>;
// }

const Canvas = (props: any) => {
  return <canvas className="canvas-size"></canvas>;
};

export default Canvas;
